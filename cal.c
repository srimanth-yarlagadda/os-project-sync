#include <stdio.h>
#include <string.h>

void merge_sort(int* array) {

}

void main(int argc, char *argv[]) {
    printf("\n\nCal started with arg 1: %s \n\n", argv[1]);
    int* p;
    // **p = (int*) argv[1];
    // printf("Pointer in cal is %p\n", p);
    
    return;
}